server:
  port: 8761
spring:
  application:
    name: scloud-eureka
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:8761
    # 注册当前实例
    fetch-registry: true
    # 发现其他实例
    register-with-eureka: true
  server:
    # 保护机制，测试时关闭保证服务及时更新
    enable-self-preservation: false
